{
  "name": "notion-sync",
  "version": "0.3.1",
  "description": "Sync Notion PAI databases to gbrain knowledge graph",
  "type": "module",
  "scripts": {
    "sync": "bun scripts/sync-pull.mjs",
    "sync:dry": "bun scripts/sync-pull.mjs --dry-run",
    "push": "bun scripts/sync.mjs",
    "push:dry": "bun scripts/sync.mjs --dry-run",
    "doctor": "node scripts/doctor.mjs",
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.15",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^22.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
